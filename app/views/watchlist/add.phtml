<h1>
	Add to Watchlist
	<a href='<?php echo $this->url->get(['for' => 'watchlist all']);?>'>*</a>
</h1>
<!-- ul>
<?php
/*
$this->url->get(['for' => 'watched']),
$this->url->get(['for' => 'watched', 'test' => 'test']),
$this->url->get(['for' => 'watched', 'test' => 123]),
$this->url->get(['for' => 'watched', 'movie_id' => $movie->_id])
*/
?>
<?php foreach ($watchlist as $movie) { ?>
	<li><?php echo $movie->title; ?>
		<a href="<?php echo $this->url->get(['for' => 'watched add', 'movie_id' => $movie->_id]); ?>">[mark as watched]</a>
		<a href="<?php echo $this->url->get(['for' => 'watchlist delete', 'movie_id' => $movie->_id]); ?>">[forget it]</a>
	</li>
<?php
//$url = $this->url->get(['for' => 'homepage']);
//$a_tag = $this->tag->linkTo(array(array('for' => 'homepage', 'id' => 'backLink'), 'back to homepage'));
?>
<?php } ?>
</ul-->

<?php /*if ($success): //Actually errors and success are both displayed by flash messages. ?>
<p>Added <span class="movie_title"><?php echo $movie->title; ?></span>. Add another?</p>
<?php else: ?>
<p>Error ($error). Try again?</p>
<?php endif; */ ?>

<p>add form</a>
<form method="post">
	<div><label>Title</label> <input name="title" /></div>
<p>Field: Movie: On /*focus*/ change, if > 3 letters, Ajax (no more than 1 request at a time) any matching movie, display on the side. Clicking the movie enters the IMDB ID in the field.</p>
	<div><label>IMDB ID</label> <input name="imdb" /></div>
	<div><label>rating</label> <input name="rating" /></div>
	<p>Helper: This regers how eager you are to watch the movie, and will be used to make recommendations.</p>
	<p>TODO: star selector.</p>
	<input type="submit" />
</form>
